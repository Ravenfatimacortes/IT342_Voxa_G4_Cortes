<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="16dp"
    android:background="@drawable/bg_stat_card"
    android:padding="16dp">

    <View
        android:id="@+id/accentLine"
        android:layout_width="4dp"
        android:layout_height="0dp"
        android:background="@color/urgent_red"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/surveyTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:text="End-of-Term Course Evaluation"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@id/tagUrgent"
        app:layout_constraintStart_toEndOf="@id/accentLine"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tagUrgent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_tag_urgent"
        android:paddingHorizontal="10dp"
        android:paddingVertical="4dp"
        android:text="Urgent"
        android:textColor="@color/urgent_red"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/surveyDesc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Share your feedback on course quality, materials, and instructor effectiveness."
        android:textColor="@color/text_secondary"
        android:textSize="13sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@id/surveyTitle"
        app:layout_constraintTop_toBottomOf="@id/surveyTitle" />

    <TextView
        android:id="@+id/questionsCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:drawableStart="@drawable/ic_questions"
        android:drawablePadding="4dp"
        android:text="12 questions"
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="@id/surveyTitle"
        app:layout_constraintTop_toBottomOf="@id/surveyDesc" />

    <TextView
        android:id="@+id/dueDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:drawableStart="@drawable/ic_time"
        android:drawablePadding="4dp"
        android:text="Due Mar 15"
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="@id/questionsCount"
        app:layout_constraintStart_toEndOf="@id/questionsCount"
        app:layout_constraintTop_toTopOf="@id/questionsCount" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:background="@drawable/bg_button_blue"
        android:paddingHorizontal="16dp"
        android:text="Take Survey"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>